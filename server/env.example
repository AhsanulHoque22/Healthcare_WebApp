# ============================================================
# ENVIRONMENT CONFIGURATION - SINGLE SOURCE OF TRUTH
# ============================================================
# Copy this file to .env and update the values for your environment
# Local Development: Set DB_HOST=localhost, DB_PORT=3306
# Docker Deployment: Set DB_HOST=mysql, DB_PORT=3306
# ============================================================

NODE_ENV=development
PORT=5000

# ============================================================
# DATABASE CONFIGURATION
# These variables are used by:
# - server/config/database.js (Sequelize connection)
# - docker-compose.yml (MySQL service & backend service)
# ============================================================
# For LOCAL DEVELOPMENT:
#   DB_HOST=localhost
#   DB_PORT=3306 (or your local MySQL port)
# For DOCKER DEPLOYMENT:
#   DB_HOST=mysql (Docker service name - do not change)
#   DB_PORT=3306
# ============================================================
DB_HOST=localhost
DB_PORT=3306
DB_NAME=healthcare_db
DB_USER=root
DB_PASSWORD=your_mysql_password

# ============================================================
# JWT CONFIGURATION
# Used by: server/middleware/auth.js
# ============================================================
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# ============================================================
# CLIENT CONFIGURATION
# Used by: 
# - server/index.js (CORS origin)
# - server/controllers/authController.js (reset password URL)
# - docker-compose.yml (backend environment)
# ============================================================
# For LOCAL: http://localhost:3000
# For DOCKER: http://localhost:3001 or your deployed domain
# For PRODUCTION: https://yourdomain.com
# ============================================================
CLIENT_URL=http://localhost:3000

# ============================================================
# EMAIL CONFIGURATION (for password reset)
# Used by: server/services/emailService.js
# ============================================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password
FROM_EMAIL=noreply@healthcare.com

# ============================================================
# FILE UPLOAD CONFIGURATION
# Used by: server/controllers/labTestController.js & prescriptionController.js
# ============================================================
MAX_FILE_SIZE=10485760
UPLOAD_PATH=uploads/

# ============================================================
# RATE LIMITING CONFIGURATION
# Used by: server/index.js
# ============================================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================
# BKASH PAYMENT CONFIGURATION (Optional)
# Used by: server/config/bkash.js & server/services/bkashService.js
# ============================================================
# BKASH_USERNAME=your_bkash_username
# BKASH_PASSWORD=your_bkash_password
# BKASH_APP_KEY=your_bkash_app_key
# BKASH_APP_SECRET=your_bkash_app_secret
# BKASH_CALLBACK_URL=http://localhost:3000/payment/callback
